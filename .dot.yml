---
role:
  pkg:
    - python
    - python3
    - name: py-pip
      os: ["alpine"]
    - name: python-pip
      os: ["arch", "centos"] # centos: epel-release
    # - name: python-dev python3-dev
    #   os: ["debian"]
    # - name: python-setuptools
    #   os: ["cygwin"]
    # TODO: virtualenv
  post_install:
    - python --version
    - python3 --version
    - pip --version
    - pip3 --version
    # - command: pip install{{if .OS "darwin"}} --user python{{end}} pip
    #   os: ["!arch"]
    # pip3: --no-cache-dir
